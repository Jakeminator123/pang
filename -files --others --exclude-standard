[1mdiff --git a/.cursorignore b/.cursorignore[m
[1mindex 206ff32..c117afe 100644[m
[1m--- a/.cursorignore[m
[1m+++ b/.cursorignore[m
[36m@@ -16,4 +16,3 @@[m [mheadless_main.py[m
 docker_1_poit[m
 headless_1_poit[m
 testmapp[m
[31m-/10_jocke/data_bundles/[m
[1mdiff --git a/1_poit/automation/process_raw_data.py b/1_poit/automation/process_raw_data.py[m
[1mindex 03702c6..47fa332 100644[m
[1m--- a/1_poit/automation/process_raw_data.py[m
[1m+++ b/1_poit/automation/process_raw_data.py[m
[36m@@ -4,7 +4,7 @@[m [mprocess_raw_data.py[m
 [m
 Bearbetar bolagsdata från info_server:[m
 - Hittar dagmapp (t.ex. info_server/20251012) och registerfilen kungorelser_YYYYMMDD.json[m
[31m-- Läser undermappar (Kxxxxxx-YY) och parsar content.txt[m
[32m+[m[32m- Läser undermappar (Kxxxxxx-25) och parsar content.txt[m
 - Skapar CSV + SQLite[m
 [m
 ZIP skapas av copy_to_dropbox.py i slutet av pipelinen.[m
[36m@@ -460,16 +460,6 @@[m [mdef main():[m
     # 2) Läs in register[m
     reg = read_json(json_path)[m
     companies = reg.get("data", [])[m
[31m-[m
[31m-    # Handle "INGA_TRAFFAR" or other non-list data (no results from search)[m
[31m-    if not isinstance(companies, list):[m
[31m-        raw_text = companies.get("raw_text", "") if isinstance(companies, dict) else ""[m
[31m-        if raw_text == "INGA_TRAFFAR":[m
[31m-            print("Inga träffar från sökning – avslutar.")[m
[31m-        else:[m
[31m-            print(f"Oväntat format på 'data' (typ: {type(companies).__name__}) – avslutar.")[m
[31m-        return[m
[31m-[m
     companies, dedup_stats = deduplicate_companies(companies)[m
     if dedup_stats["removed"] or dedup_stats["filtered_keywords"]:[m
         print([m
[1mdiff --git a/1_poit/automation/scrape_kungorelser.py b/1_poit/automation/scrape_kungorelser.py[m
[1mindex a1f319e..045ce2a 100644[m
[1m--- a/1_poit/automation/scrape_kungorelser.py[m
[1m+++ b/1_poit/automation/scrape_kungorelser.py[m
[36m@@ -13,14 +13,10 @@[m [mimport time[m
 from datetime import datetime, timedelta[m
 from pathlib import Path[m
 [m
[31m-# Fixa encoding och buffering för Windows-terminal[m
[31m-# VIKTIGT: line_buffering=True för att se output i realtid[m
[32m+[m[32m# Fixa encoding för Windows-terminal[m
 if sys.platform == "win32":[m
[31m-    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8", errors="replace", line_buffering=True)[m
[31m-    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8", errors="replace", line_buffering=True)[m
[31m-[m
[31m-# Sätt PYTHONUNBUFFERED för att säkerställa ingen buffring[m
[31m-os.environ["PYTHONUNBUFFERED"] = "1"[m
[32m+[m[32m    sys.stdout = io.TextIOWrapper(sys.stdout.buffer, encoding="utf-8", errors="replace")[m
[32m+[m[32m    sys.stderr = io.TextIOWrapper(sys.stderr.buffer, encoding="utf-8", errors="replace")[m
 [m
 import cv2 as cv[m
 import numpy as np[m
[36m@@ -32,16 +28,6 @@[m [mpg.FAILSAFE = True[m
 import mss[m
 import pygetwindow as gw[m
 [m
[31m-# --- DPI awareness (Windows) ---[m
[31m-if sys.platform == "win32":[m
[31m-    try:[m
[31m-        ctypes.windll.shcore.SetProcessDpiAwareness(2)[m
[31m-    except Exception:[m
[31m-        try:[m
[31m-            ctypes.windll.user32.SetProcessDPIAware()[m
[31m-        except Exception:[m
[31m-            pass[m
[31m-[m
 # ===========================[m
 # Konfiguration[m
 # ===========================[m
[36m@@ -153,49 +139,25 @@[m [mIMG_POPUP = str((COOKIE_DIR / "popup.jpg").resolve())[m
 IMG_OK = str((COOKIE_DIR / "ok.jpg").resolve())[m
 [m
 SOK_DIR = BASE_DIR / "bilder" / "2_sok_kunngorelse"[m
[31m-IMG_LANK = str((SOK_DIR / "lank.jpg").resolve())  # Huvudlänk-bild[m
[31m-IMG_LANK_ALT = str((SOK_DIR / "alternativ_lank.jpg").resolve())  # Hoppa om redan på söksidan[m
[31m-IMG_LANK_LAPTOP = str((SOK_DIR / "laptop_sok_kungorelse.jpg").resolve())  # Laptop-specifik bild[m
[31m-[m
[31m-OVRIGT_DIR = BASE_DIR / "bilder" / "4_ovrigt"[m
[31m-IMG_OK_FORTSATT = str((OVRIGT_DIR / "ok_fortsatt.jpg").resolve())  # Banner "ok, fortsätt" efter länk-klick[m
[32m+[m[32mIMG_LANK = str((SOK_DIR / "lank.jpg").resolve())  # kräver ≥ 0.88[m
[32m+[m[32mIMG_LANK_ALT = str((SOK_DIR / "alternativ_lank.jpg").resolve())  # hoppa klick om redan på söksidan[m
 [m
 MENY_DIR = BASE_DIR / "bilder" / "3_menyer"[m
 MENY_GLOB = "*.*"  # jpg/png/jpeg[m
 [m
[31m-# UI elements that can scale with DPI/zoom[m
[31m-# BRETT intervall för olika skärmar (1/6 storlek = ca 0.16 skala)[m
[31m-SCALES_UI = [round(x, 2) for x in np.arange(0.15, 2.05, 0.05)][m
[31m-[m
[31m-# ===========================[m
[31m-# KONTROLLERA COOKIE-HANTERING[m
[31m-# ===========================[m
[31m-# Cookie-banner kommer ibland men orsakar falska matchningar vid skala 0.15[m
[31m-# Inaktiveras tills vidare - popup dyker upp så sällan att det inte är värt[m
[31m-SKIP_COOKIE_CHECK = True   # INAKTIVERAD - orsakar falska matchningar[m
[31m-SKIP_OK_FORTSATT = True    # INAKTIVERAD - orsakar problem[m
[31m-[m
[31m-# Trösklar - baserat på test_match.py resultat:[m
[31m-# - laptop_sok_kungorelse.jpg: score 1.000[m
[31m-# - lank.jpg: score 0.937[m
[31m-# Använder säkra trösklar under dessa värden[m
[31m-[m
[31m-CONF_POPUP = 0.80  # Cookie-popup (om den finns ska den vara tydlig)[m
[31m-CONF_OK = 0.80     # OK-knapp i cookie-popup[m
[31m-[m
[31m-# LÄNK: Test visade 0.937-1.000, så 0.70 är säker marginal[m
[31m-CONF_LANK = 0.70   # Länk till "Sök kungörelse"[m
[31m-[m
[31m-# ÖVRIGA[m
[31m-CONF_OK_FORTSATT = 0.85  # Banner efter länk-klick[m
[31m-CONF_MENY_GRAY = 0.60    # Meny-element[m
[31m-CONF_MENY_EDGE = 0.60[m
[31m-CONF_MENY_ORB = 0.40[m
[32m+[m[32m# Trösklar[m
[32m+[m[32m# Sänkta trösklar för bättre matchning vid olika skärminställningar[m
[32m+[m[32mCONF_POPUP = 0.78[m
[32m+[m[32mCONF_OK = 0.80[m
[32m+[m[32mCONF_LANK = 0.82[m
[32m+[m[32mCONF_MENY_GRAY = 0.72  # Sänkt från 0.86 - hanterar DPI-skalning/ljusskillnader bättre[m
[32m+[m[32mCONF_MENY_EDGE = 0.75  # Sänkt från 0.82[m
[32m+[m[32mCONF_MENY_ORB = 0.50  # inlier-ratio, sänkt för mer flexibilitet[m
 [m
 # Tidsouts & beteenden[m
 WINDOW_FIND_TIMEOUT = 8.0[m
[31m-POPUP_TIMEOUT_SEC = 15.0  # 15 sekunder för cookie-banner (som användaren önskade)[m
[31m-STEP_TIMEOUT = 15.0       # Timeout för meny-steg[m
[32m+[m[32mPOPUP_TIMEOUT_SEC = 12.0  # <= 12s[m
[32m+[m[32mSTEP_TIMEOUT = 12.0[m
 POST_CLICK_WAIT = (1.2, 1.4)  # Halverat från (1.0, 2.0)[m
 STRICT_SEQUENCE = True[m
 [m
[36m@@ -217,24 +179,12 @@[m [mWAIT_MOUSE_SHORT = 0.25  # Kort väntan vid musrörelse[m
 WAIT_SCROLL_SHORT = 0.25  # Kort väntan vid scroll[m
 [m
 # Multiskala[m
[31m-SCALES_LANK = SCALES_UI[m
[32m+[m[32mSCALES_LANK = [0.95, 1.00, 1.05][m
 SAMPLES_LANK = 5[m
 LANK_TIMEOUT = 6.0[m
 [m
 # Utökat skalintervall för bättre matchning vid olika DPI-inställningar[m
[31m-# Använd samma breda intervall som SCALES_UI för konsistens[m
[31m-SCALES_MENY = SCALES_UI[m
[31m-[m
[31m-# ===========================[m
[31m-# Sökområden - begränsa var vi letar efter element[m
[31m-# ===========================[m
[31m-# Länk-element (lank.jpg, alternativ_lank.jpg) finns alltid i:[m
[31m-# X: 10% till 45% av fönsterbredden[m
[31m-# Y: topp 40% av fönsterhöjden[m
[31m-LANK_REGION_X_START = 0.10  # 10% från vänster[m
[31m-LANK_REGION_X_END = 0.45    # 45% från vänster[m
[31m-LANK_REGION_Y_START = 0.0   # Från toppen[m
[31m-LANK_REGION_Y_END = 0.40    # 40% från toppen[m
[32m+[m[32mSCALES_MENY = [round(x, 2) for x in np.arange(0.70, 1.35, 0.03)][m
 [m
 # Klick-skydd[m
 TITLEBAR_GUARD = 40[m
[36m@@ -862,36 +812,6 @@[m [mdef refresh_region(win):[m
         return None[m
 [m
 [m
[31m-def get_link_search_region(full_region):[m
[31m-    """[m
[31m-    Beräkna sökregion för länk-element (lank.jpg, alternativ_lank.jpg).[m
[31m-    Länken finns alltid i:[m
[31m-    - X: 10% till 45% av fönsterbredden[m
[31m-    - Y: topp 40% av fönsterhöjden[m
[31m-    [m
[31m-    Args:[m
[31m-        full_region: (left, top, width, height) av hela fönstret[m
[31m-    [m
[31m-    Returns:[m
[31m-        (left, top, width, height) för sökregionen[m
[31m-    """[m
[31m-    if full_region is None:[m
[31m-        return None[m
[31m-    [m
[31m-    win_left, win_top, win_width, win_height = full_region[m
[31m-    [m
[31m-    # Beräkna sub-region[m
[31m-    sub_left = int(win_left + win_width * LANK_REGION_X_START)[m
[31m-    sub_top = int(win_top + win_height * LANK_REGION_Y_START)[m
[31m-    sub_width = int(win_width * (LANK_REGION_X_END - LANK_REGION_X_START))[m
[31m-    sub_height = int(win_height * (LANK_REGION_Y_END - LANK_REGION_Y_START))[m
[31m-    [m
[31m-    print(f"[REGION] Länk-sökområde: x={sub_left}, y={sub_top}, w={sub_width}, h={sub_height}")[m
[31m-    print(f"[REGION] (Fönster: x={win_left}, y={win_top}, w={win_width}, h={win_height})")[m
[31m-    [m
[31m-    return (sub_left, sub_top, sub_width, sub_height)[m
[31m-[m
[31m-[m
 def goto_url(url: str, win=None):[m
     """Navigera till en URL med förbättrad adressfältshantering"""[m
     check_pause()  # Kolla om pausad[m
[36m@@ -983,38 +903,31 @@[m [mdef read_template_gray(path_str):[m
     return cv.imread(path_str, cv.IMREAD_GRAYSCALE)[m
 [m
 [m
[31m-def match_best(screen_gray, templ_gray, scale=1.0, normalize=True, blur_ksize=5):[m
[32m+[m[32mdef match_best(screen_gray, templ_gray, scale=1.0, normalize=True):[m
     """[m
[31m-    Template matching robust mot DPI/antialias/kontrast.[m
[32m+[m[32m    Template matching med histogram equalization för bättre robusthet[m
[32m+[m[32m    mot olika skärminställningar (ljusstyrka, kontrast, DPI).[m
     """[m
     t = templ_gray[m
     s = screen_gray[m
 [m
[32m+[m[32m    # Normalisera ljusstyrka med histogram equalization (CLAHE)[m
     if normalize:[m
         clahe = cv.createCLAHE(clipLimit=2.0, tileGridSize=(8, 8))[m
         s = clahe.apply(s)[m
         t = clahe.apply(t)[m
 [m
[31m-    if blur_ksize and blur_ksize >= 3:[m
[31m-        if blur_ksize % 2 == 0:[m
[31m-            blur_ksize += 1[m
[31m-        s = cv.GaussianBlur(s, (blur_ksize, blur_ksize), 0)[m
[31m-        t = cv.GaussianBlur(t, (blur_ksize, blur_ksize), 0)[m
[31m-[m
     if scale != 1.0:[m
         h, w = t.shape[:2][m
[31m-        new_w = max(1, int(w * scale))[m
[31m-        new_h = max(1, int(h * scale))[m
         t = cv.resize([m
             t,[m
[31m-            (new_w, new_h),[m
[32m+[m[32m            (int(w * scale), int(h * scale)),[m
             interpolation=cv.INTER_AREA if scale < 1 else cv.INTER_CUBIC,[m
         )[m
[31m-[m
     if s.shape[0] < t.shape[0] or s.shape[1] < t.shape[1]:[m
         return None, None, None[m
     res = cv.matchTemplate(s, t, cv.TM_CCOEFF_NORMED)[m
[31m-    _, max_val, _, max_loc = cv.minMaxLoc(res)[m
[32m+[m[32m    min_val, max_val, min_loc, max_loc = cv.minMaxLoc(res)[m
     th, tw = t.shape[:2][m
     return max_val, max_loc, (tw, th)[m
 [m
[36m@@ -1028,110 +941,47 @@[m [mdef ensure_saved(path, img_bgr):[m
 [m
 [m
 # ===========================[m
[31m-# Länk (bästa-av-flera samples)[m
[32m+[m[32m# Länk (≥ 0.88, bästa-av-flera, 1s mellan frames)[m
 # ===========================[m
 def locate_best_over_samples([m
     img_path, window_region, threshold, timeout_sec, scales, samples[m
 ):[m
[31m-    """[m
[31m-    Sök efter en bild i window_region och returnera den BÄSTA matchningen.[m
[31m-    [m
[31m-    VIKTIGT: Tar alltid den HÖGSTA poängen, inte första som passerar threshold.[m
[31m-    [m
[31m-    Args:[m
[31m-        img_path: Sökväg till template-bild[m
[31m-        window_region: (left, top, width, height) - SKÄRMKOORDINATER att söka i[m
[31m-        threshold: Minsta poäng för godkänd matchning[m
[31m-        timeout_sec: Max tid att söka[m
[31m-        scales: Lista av skalor att prova[m
[31m-        samples: Antal frames att ta[m
[31m-    [m
[31m-    Returns:[m
[31m-        (box, score) eller (None, None) om ingen matchning över threshold[m
[31m-    """[m
[31m-    img_name = Path(img_path).name[m
     templ = read_template_gray(img_path)[m
     if templ is None:[m
[31m-        print(f"[FEL] Kan inte läsa mall: {img_path}")[m
[32m+[m[32m        print(f"[FEL] Kan inte läsa: {img_path}")[m
         return None, None[m
[31m-    [m
[31m-    templ_h, templ_w = templ.shape[:2][m
[31m-    L, T, W, H = window_region[m
[31m-    [m
[31m-    print(f"")[m
[31m-    print(f"[MATCH] ========================================")[m
[31m-    print(f"[MATCH] Söker: '{img_name}'")[m
[31m-    print(f"[MATCH] Template: {templ_w}x{templ_h} pixlar")[m
[31m-    print(f"[MATCH] Sökområde: x={L}, y={T}, w={W}, h={H}")[m
[31m-    print(f"[MATCH] Skala: {min(scales):.2f} - {max(scales):.2f} ({len(scales)} steg)")[m
[31m-    print(f"[MATCH] Threshold: {threshold:.2f}")[m
[31m-    print(f"[MATCH] ========================================")[m
[31m-    [m
[31m-    # SPARA debug-bild av sökområdet INNAN sökning[m
[31m-    ts = datetime.now().strftime("%Y%m%d_%H%M%S")[m
[31m-    [m
     t_end = time.time() + timeout_sec[m
[31m-    best_score, best_box, best_frame, best_scale, best_loc_in_frame = -1.0, None, None, None, None[m
[32m+[m[32m    best_score, best_box, best_frame = -1.0, None, None[m
     frames = 0[m
[31m-    [m
     while frames < samples and time.time() < t_end:[m
[31m-        bgr = grab_region_bgr_any(window_region)[m
[32m+[m[32m        bgr = grab_region_bgr_any(window_region)  # throttlad[m
         gray = cv.cvtColor(bgr, cv.COLOR_BGR2GRAY)[m
[31m-        [m
[31m-        # Spara första frame som debug[m
[31m-        if frames == 0:[m
[31m-            debug_path = str(Path(DEBUG_DIR) / f"search_area_{img_name.replace('.', '_')}_{ts}.png")[m
[31m-            ensure_saved(debug_path, bgr)[m
[31m-            print(f"[MATCH] Debug-bild sparad: {debug_path}")[m
[31m-        [m
         for sc in scales:[m
[31m-            score, loc, dims = match_best(gray, templ, scale=sc)[m
[31m-            if score is None or dims is None:[m
[32m+[m[32m            score, loc, (tw, th) = match_best(gray, templ, scale=sc)[m
[32m+[m[32m            if score is None:[m
                 continue[m
[31m-            tw, th = dims[m
[31m-            [m
[31m-            # ALLTID spara den bästa matchningen[m
             if score > best_score:[m
[32m+[m[32m                L, T, W, H = window_region[m
                 x, y = loc[m
[31m-                # Box i SKÄRMKOORDINATER[m
[31m-                best_score = score[m
[31m-                best_box = (L + x, T + y, tw, th)[m
[31m-                best_frame = bgr.copy()[m
[31m-                best_scale = sc[m
[31m-                best_loc_in_frame = (x, y, tw, th)[m
[31m-                [m
[32m+[m[32m                best_score, best_box, best_frame = score, (L + x, T + y, tw, th), bgr[m
         frames += 1[m
[31m-        if frames < samples:[m
[31m-            time.sleep(max(0.0, FRAME_GAP_SEC))[m
[31m-    [m
[31m-    # Logga resultat[m
[31m-    print(f"[MATCH] ----------------------------------------")[m
[31m-    if best_scale is not None:[m
[31m-        print(f"[MATCH] Bästa score: {best_score:.3f} (skala={best_scale:.2f})")[m
[31m-        print(f"[MATCH] Position i frame: x={best_loc_in_frame[0]}, y={best_loc_in_frame[1]}")[m
[31m-        print(f"[MATCH] Position på skärm: x={best_box[0]}, y={best_box[1]}")[m
[31m-    else:[m
[31m-        print(f"[MATCH] Ingen matchning hittad för '{img_name}'")[m
[31m-    [m
[31m-    # SPARA alltid debug-bild med markering av var bästa matchningen är[m
[31m-    if best_frame is not None and best_loc_in_frame is not None:[m
[31m-        x, y, tw, th = best_loc_in_frame[m
[31m-        out = best_frame.copy()[m
[31m-        # Rita rektangel runt matchningen[m
[31m-        color = (0, 255, 0) if best_score >= threshold else (0, 0, 255)  # Grön=OK, Röd=Under threshold[m
[31m-        cv.rectangle(out, (x, y), (x + tw, y + th), color, 2)[m
[31m-        # Skriv score på bilden[m
[31m-        cv.putText(out, f"score={best_score:.3f}", (x, y - 10), cv.FONT_HERSHEY_SIMPLEX, 0.5, color, 1)[m
[31m-        debug_match_path = str(Path(DEBUG_DIR) / f"match_{img_name.replace('.', '_')}_{best_score:.3f}_{ts}.png")[m
[31m-        ensure_saved(debug_match_path, out)[m
[31m-    [m
[32m+[m[32m        time.sleep(max(0.0, FRAME_GAP_SEC))  # 1s fri innan nästa frame[m
     if best_score >= threshold:[m
[31m-        print(f"[MATCH] ✓ GODKÄND (score {best_score:.3f} >= threshold {threshold:.2f})")[m
[31m-        print(f"[MATCH] ========================================")[m
         return best_box, best_score[m
[31m-    [m
[31m-    print(f"[MATCH] ✗ UNDERKÄND (score {best_score:.3f} < threshold {threshold:.2f})")[m
[31m-    print(f"[MATCH] ========================================")[m
[32m+[m[32m    # debug om miss[m
[32m+[m[32m    ts = datetime.now().strftime("%Y%m%d_%H%M%S")[m
[32m+[m[32m    if best_frame is None:[m
[32m+[m[32m        best_frame = grab_region_bgr_any(window_region)[m
[32m+[m[32m    ensure_saved(str(Path(DEBUG_DIR) / f"lank_window_{ts}.png"), best_frame)[m
[32m+[m[32m    if best_box:[m
[32m+[m[32m        L, T, W, H = best_box[m
[32m+[m[32m        x = L - window_region[0][m
[32m+[m[32m        y = T - window_region[1][m
[32m+[m[32m        out = best_frame.copy()[m
[32m+[m[32m        cv.rectangle(out, (x, y), (x + W, y + H), (0, 0, 255), 2)[m
[32m+[m[32m        ensure_saved([m
[32m+[m[32m            str(Path(DEBUG_DIR) / f"lank_best_below_{best_score:.3f}_{ts}.png"), out[m
[32m+[m[32m        )[m
     return None, None[m
 [m
 [m
[36m@@ -1139,30 +989,20 @@[m [mdef locate_best_over_samples([m
 # Meny-matchning (grå+edge+ev. ORB), 1s mellan frames[m
 # ===========================[m
 def locate_menu_robust(img_path: str, window_region, timeout_sec: float):[m
[31m-    img_name = Path(img_path).name[m
     templ = read_template_gray(img_path)[m
     if templ is None:[m
[31m-        print(f"[MENY] Kan inte läsa template: {img_path}")[m
         return None, None, None, None[m
[31m-    [m
[31m-    templ_h, templ_w = templ.shape[:2][m
[31m-    print(f"[MENY] Söker '{img_name}' (template {templ_w}x{templ_h}px)")[m
[31m-    print(f"[MENY] Skalintervall: {min(SCALES_MENY):.2f} - {max(SCALES_MENY):.2f} ({len(SCALES_MENY)} steg)")[m
[31m-    [m
     t_end = time.time() + timeout_sec[m
     best = (-1.0, None, None, None, None)  # score, box, scale, mode, frame[m
[31m-    iteration = 0[m
     while time.time() < t_end:[m
[31m-        iteration += 1[m
         bgr = grab_region_bgr_any(window_region)  # throttlad[m
         gray = cv.cvtColor(bgr, cv.COLOR_BGR2GRAY)[m
 [m
         # Gråskala[m
         for sc in SCALES_MENY:[m
[31m-            score, loc, dims = match_best(gray, templ, scale=sc)[m
[31m-            if score is None or dims is None:[m
[32m+[m[32m            score, loc, (tw, th) = match_best(gray, templ, scale=sc)[m
[32m+[m[32m            if score is None:[m
                 continue[m
[31m-            tw, th = dims[m
             if score > best[0]:[m
                 L, T, W, H = window_region[m
                 x, y = loc[m
[36m@@ -1172,10 +1012,9 @@[m [mdef locate_menu_robust(img_path: str, window_region, timeout_sec: float):[m
         edges_scr = cv.Canny(gray, 50, 150)[m
         edges_tpl = cv.Canny(templ, 50, 150)[m
         for sc in SCALES_MENY:[m
[31m-            score, loc, dims = match_best(edges_scr, edges_tpl, scale=sc, normalize=False, blur_ksize=0)[m
[31m-            if score is None or dims is None:[m
[32m+[m[32m            score, loc, (tw, th) = match_best(edges_scr, edges_tpl, scale=sc)[m
[32m+[m[32m            if score is None:[m
                 continue[m
[31m-            tw, th = dims[m
             if score > best[0]:[m
  