# =============================================================================
# CHROME PROFIL-KONFIGURATION
# =============================================================================
# Kopiera denna fil till .env och justera efter behov.
#
# Dessa inställningar styr hur Chrome-profiler hanteras för att:
# - Undvika att profiler blir enorma (100MB+ cache)
# - Behålla login/session (cookies, localStorage)
# - Förhindra "Återställ session"-problem
#
# PROFILER FINNS HÄR:
# - GUI-scraping:     1_poit/automation/chrome_profile
# - Headless-scraping: headless_1_poit/chrome_profile
# =============================================================================

# --- AUTOMATISK CACHE-RENSNING ---

# Aktivera automatisk cache-rensning vid pipeline-start? (true/false)
# Om true: Rensar cache-mappar men BEHÅLLER cookies/session
CHROME_CACHE_CLEANUP_ENABLED=true

# Max profilstorlek i MB innan varning visas
# Om profilen överstiger detta visas en varning i loggen
CHROME_PROFILE_MAX_SIZE_MB=200

# --- VAD SOM RENSAS (true = rensas, false = behålls) ---

# HTTP-cache (största boven, 100-500MB)
CHROME_CLEAN_HTTP_CACHE=true

# JavaScript Code Cache (kompilerad JS, 10-50MB)
CHROME_CLEAN_CODE_CACHE=true

# GPU Cache (texturer, 10-50MB)
CHROME_CLEAN_GPU_CACHE=true

# Service Worker Cache (PWA-data, kan bli stort)
CHROME_CLEAN_SERVICE_WORKER=true

# Blob Storage (temporär data)
CHROME_CLEAN_BLOB_STORAGE=true

# --- SÄKERHETSINSTÄLLNINGAR (VAD SOM ALDRIG RENSAS) ---
# Dessa rensas ALDRIG oavsett inställningar ovan:
# - Cookies (login-session)
# - Local Storage (sessionsdata)
# - Preferences (profilinställningar)
# - Login Data (sparade lösenord)
# - Extensions/ (installerade tillägg)

# --- FULLSTÄNDIG PROFILRENSNING ---

# Aktivera fullständig profilrensning? (FÖRSIKTIGHET!)
# Om true: Raderar HELA profilen inklusive cookies
# Användning: python utils/erase.py --chrome-full
# VARNING: Du måste logga in igen efter detta!
CHROME_ALLOW_FULL_RESET=false

# --- TIDSINSTÄLLNINGAR FÖR SCRAPING ---

# Väntetid per sida (sekunder) - min och max för random
SCRAPE_WAIT_MIN=4
SCRAPE_WAIT_MAX=6

# Väntetid mellan sidor (sekunder) - min och max för random
SCRAPE_BETWEEN_MIN=2
SCRAPE_BETWEEN_MAX=4

# Cookie-banner väntetid (sekunder) - efter klick på "Acceptera"
SCRAPE_COOKIE_WAIT=10

# --- PORT OCH KONFLIKT-HANTERING ---

# Chrome remote debugging port (ändra om 9222 är upptagen)
# OBS: Om du ändrar här måste du också starta Chrome manuellt med samma port
CHROME_DEBUG_PORT=9222

# Timeout för scrape-lock (sekunder) - efter denna tid anses lock vara "stale"
SCRAPE_LOCK_TIMEOUT=7200

# =============================================================================
# DASHBOARD UPLOAD-KONFIGURATION
# =============================================================================
# Automatisk uppladdning av ZIP-bundles till dashboard (10_jocke/dashboard)
# 
# Om UPLOAD_SECRET är satt körs uppladdning automatiskt som sista steg i main.py
# Dashboard hostad på Render.com: https://jocke-dashboard.onrender.com
# =============================================================================

# API-nyckel för dashboard-uppladdning
# Måste matcha UPLOAD_SECRET (eller JOCKE_API) i Render environment variables
# Lämna tom för att hoppa över automatisk uppladdning
UPLOAD_SECRET=

# Dashboard URL (om du kör egen instans)
# Standard: https://jocke-dashboard.onrender.com
# DASHBOARD_URL=http://localhost:3000

